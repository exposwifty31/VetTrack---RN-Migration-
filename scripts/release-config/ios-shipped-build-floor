# The highest iOS build number App Store Connect has ACCEPTED.
# Offline oracle for the build-number collision gate — nothing offline can ask ASC.
# Raise this when App Store Connect ACCEPTS a build, from EITHER lane: this app
# and the Capacitor shell share bundle id uk.vettrack.app, so they share one
# CFBundleVersion counter. NOT on upload — an internal EAS `preview` build
# consumes the number in EAS but never reaches ASC, and must not move this file.
# Update vettrack/ios/.last-shipped-build to the same number: each lane's gate
# reads its own copy, so the two diverging re-creates the collision this prevents.
28
